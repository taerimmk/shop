<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.june.shop.site.repository.SampleRepository">

  

  <select id="sampleList" resultType="com.june.shop.site.model.Owner">
    SELECT
    	address
    	,city
        ,telephone  
    FROM owners
  </select>

</mapper>